version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 20
    commands:
      - npm ci
  build:
    commands:
      - npm run build

artifacts:
  base-directory: .
  files:
    - '**/*'
  exclude:
    - node_modules/**
    - '.git/**'
    - '.github/**'
